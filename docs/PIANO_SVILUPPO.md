# GM-Trekking — Piano di sviluppo

App Android per pianificare percorsi di trekking a partire da tracciati GPX, navigare in tempo reale con il GPS e trovare luoghi utili (ristoranti, bar, hotel, ostelli, rifugi) lungo il percorso.

Riferimento: analisi di fattibilità e ricerca di mercato (documento separato, condiviso in chat). Questo piano la traduce in fasi di sviluppo concrete.

## Principio guida: sicurezza e chiarezza per utenti non esperti

Questo è il criterio prioritario di progettazione dell'intera app, non una rifinitura da aggiungere dopo. Ogni schermata di navigazione deve rispettare:

- **Indicazione di direzione grande e immediata**: freccia ben visibile verso il prossimo punto del tracciato, distanza residua in metri, nessun gergo tecnico (niente "bearing", "waypoint", "azimut" nell'interfaccia).
- **Zoom automatico nei punti critici**: la mappa si ingrandisce da sola in prossimità di bivi, incroci di sentieri o tratti con tracciati ravvicinati — lo stesso pattern dei navigatori auto agli incroci.
- **Segnalazione "fuori percorso" azionabile**: non un semplice allarme, ma un'istruzione chiara su come tornare sul tracciato.
- **Vista d'insieme vs vista di dettaglio**: overview del percorso nei tratti lineari, passaggio automatico alla vista ravvicinata nei tratti complessi.
- **Interfaccia essenziale**: pochi elementi a schermo, icone semplici, testo minimo.

Questi punti sono criteri di accettazione della fase MVP, non funzionalità opzionali.

## Fase 0 — Setup

- [x] Repository GitHub creato (`gmregservice/GM-Trekking`)
- [x] Scheletro progetto Android Kotlin + Jetpack Compose (mappa principale con posizione corrente e navigazione, Luoghi utili; motore di navigazione; integrazione MapLibre; client Overpass API; servizio di localizzazione in foreground)
- [x] Pipeline GitHub Actions per build automatica dell'APK di debug (e, su richiesta, di release non firmata)
- [x] Tema grafico bianco applicato a tutte le schermate
- [x] Primo push e primo build verde su GitHub Actions
- [x] APK installata e avviata su dispositivo reale (workflow di sviluppo: solo GitHub Actions, senza Android Studio)
- [x] Caricamento GPX reso opzionale: l'app si apre sulla mappa con la posizione corrente, non forza più il caricamento di un percorso prima di mostrare qualcosa
- [x] Schermata di crash in-app (mostra l'errore a schermo, con opzione "Condividi"): necessaria perché senza Android Studio non c'è modo di leggere Logcat
- [x] Firma APK di debug stabilizzata (`keystore/debug.keystore` fissa): prima ogni build GitHub Actions firmava l'APK con una chiave diversa e le build successive alla prima non si installavano più sopra quella vecchia
- [x] Mappa reale sostituita allo stile dimostrativo: fuori casa la mappa restava vuota (solo colore di sfondo, nessun dettaglio) perché lo stile demo di MapLibre non ha dati oltre i confini nazionali — sostituito con OpenFreeMap (`liberty`)

## Fase 1 — MVP (obiettivo: 4-6 mesi, team 1-2 sviluppatori)

Funzionalità minime per un prodotto usabile e sicuro:

1. **Import GPX (opzionale)**: caricamento di un file `.gpx` dal dispositivo, parsing e visualizzazione del tracciato su mappa offline. L'app si apre comunque mostrando la posizione corrente sulla mappa anche senza un percorso caricato: caricarne uno è un'azione che l'utente sceglie di fare, non un passaggio obbligato.
2. **Navigazione GPS sul tracciato**: posizione utente in tempo reale sovrapposta al tracciato, calcolo continuo della distanza dal percorso, avviso chiaro di deviazione, freccia direzionale e zoom automatico nei punti critici (vedi principio guida sopra). [x] Pulsante "Ricentra" sulla mappa: dopo aver scorso la mappa (es. per vedere cosa c'è più avanti lungo il percorso), riporta la visualizzazione sulla posizione corrente senza doverla cercare manualmente.
   - **Ricentraggio automatico allo sblocco dello schermo** — [x] **fatto (v1.16)**: segnalato che a schermo spento la posizione mostrata restava ferma, e occorreva premere manualmente "Ricentra" dopo aver sbloccato il telefono. Verificata l'architettura del servizio di localizzazione in foreground (`LocationTrackingService.kt`, `startForeground`/`FusedLocationProviderClient`/priorità alta): raccoglie posizione correttamente anche a schermo spento — il problema non era nella raccolta dei dati ma nella fotocamera della mappa, che restava ferma dov'era all'ultimo sblocco invece di seguire di nuovo la posizione. Corretto in `MainMapScreen.kt` con un `DisposableEffect` che osserva il ciclo di vita della schermata (`Lifecycle.Event.ON_RESUME`, l'evento generato quando il telefono viene sbloccato con l'app già in primo piano) e forza un ricentraggio automatico ad ogni sblocco. Non implementato un tracciamento GPS realmente continuo anche a schermo spento (richiederebbe il permesso `ACCESS_BACKGROUND_LOCATION`, più invasivo e con un impatto sul consumo batteria da valutare con test sul campo): scelta la soluzione più contenuta, esplicitamente indicata come alternativa accettabile.
   - **Freccia sulla mappa, orientata rispetto al senso di marcia, e bussola sovrapposta** — [x] **fatto (v1.18)**: segnalati due problemi distinti (agosto 2026). Primo, la mappa occupava poco più di metà schermo durante la navigazione, perché il blocco freccia sotto la mappa aveva un'altezza fissa indipendente da essa. Secondo, e più importante, la freccia ruotava rispetto al nord (bearing assoluto verso il prossimo punto), non rispetto a dove l'utente stava effettivamente guardando/camminando — un limite già annotato nel codice fin dalla prima versione della freccia, mai affrontato prima: risultato, in un bivio la freccia poteva indicare "destra" anche quando il punto di arrivo era realmente a sinistra, e seguirla poteva allontanare dalla meta invece di avvicinare.
     - **Freccia spostata sulla mappa** (`TrekMapView.kt`): non più un blocco fisso sotto la mappa, ma un'icona rossa (bordo bianco, per risaltare sia su sfondo verde/bosco sia su azzurro/acqua) disegnata via `Canvas` a runtime (nessuna risorsa immagine aggiunta al progetto) e ancorata alle coordinate GPS reali tramite un `SymbolLayer` di MapLibre con `iconRotate` dinamico — resta allineata alla vera posizione anche scorrendo/zoomando la mappa, a differenza di un overlay fisso al centro schermo. Sostituisce il cerchio "posizione" solo durante la navigazione attiva; fuori navigazione il cerchio resta come prima. La mappa ora occupa sempre tutto lo spazio verticale disponibile (`weight(1f)`, prima era un'altezza fissa di 280dp quando un percorso era caricato), perché il pannello sotto è diventato compatto (solo le due righe di testo con le distanze, niente più la freccia).
     - **Orientamento rispetto al senso di marcia, non al nord** (`DeviceHeading.kt`, nuovo): l'angolo mostrato ora è `bearing verso il prossimo punto − orientamento del telefono`, non più il bearing da solo. L'orientamento del telefono viene letto dal sensore combinato `Sensor.TYPE_ROTATION_VECTOR` (accelerometro + magnetometro + giroscopio), con la correzione per la rotazione dello schermo (`remapCoordinateSystem`). Se il sensore manca sul dispositivo, si torna al comportamento precedente (rotazione rispetto al nord) invece di un errore — stesso principio già usato per il contapassi.
     - **Bussola, non media col GPS — scelta deliberata**: l'alternativa sarebbe stata calcolare la direzione di marcia dagli spostamenti GPS successivi (`Location.getBearing()`), ma è nota per essere inaffidabile proprio a passo d'uomo (velocità bassa, soste frequenti) — lo scenario tipico di un'escursione. La bussola funziona anche da fermi. Se in futuro si rivelasse insufficiente (es. interferenze magnetiche in certe zone), una combinazione dei due resta un miglioramento possibile, non implementato ora per contenere il rischio di questo incremento.
     - **Bussola sovrapposta alla mappa** (`CompassOverlay.kt`, nuovo): icona "N" che ruota per restare allineata al nord reale, angolo opposto al pulsante "Ricentra" per non sovrapporsi, usa lo stesso sensore di orientamento. Tocca per nascondere/mostrare (l'opzione "Visualizza sì/no" richiesta): da nascosta resta comunque un piccolo cerchio toccabile nello stesso punto, non sparisce del tutto.
     - **Zoom automatico nei punti critici**: non toccato, resta esattamente come prima (richiesto esplicitamente di mantenerlo invariato).
   - **Punti cardinali sulla bussola, titolo del percorso in navigazione, fix z-order freccia/tracciato** — [x] **fatto (v1.19)**: dopo aver verificato sul campo la freccia sulla mappa (v1.18, giudicata "ben visibile"), segnalate due richieste. Prima, semplice: aggiungere i punti cardinali alla bussola. Seconda, più importante e segnalata come "fondamentale, senza questa la navigazione risulta inutile": durante la navigazione verso un luogo o un percorso non compariva alcuna indicazione di quale tracciato seguire, né (per i sentieri) il suo nome/numero.
     - **Punti cardinali N/E/S/O** (`CompassOverlay.kt`): le quattro lettere ruotano insieme come un unico quadrante (non una singola freccia con etichette fisse), stesso principio di una bussola vera — il quadrante gira, non l'ago. Da nascosta resta comunque l'icona a freccia singola già presente in v1.18.
     - **Titolo di cosa si sta seguendo** (`MainMapScreen.kt`): sopra alle distanze compare ora "Stai andando verso: [nome luogo]" (già presente) oppure, novità di questa versione, "Stai seguendo: [nome percorso]" quando la guida è un tracciato GPX caricato o un sentiero scelto da "Sentieri vicini" — prima non compariva nulla in questo secondo caso.
     - **Instradamento reale (linea guida sul percorso da seguire)**: in questa versione (v1.19) non ancora incluso — vedi punto 13 più sotto per l'implementazione effettiva, arrivata in v1.24 dopo la configurazione della chiave API (v1.20).
     - **Fix z-order (bug reale, non richiesto dall'utente, trovato durante questa modifica)**: in `TrekMapView.kt` la freccia/il cerchio di posizione venivano aggiunti alla mappa PRIMA del layer del tracciato, quindi in alcuni tratti la linea del percorso poteva disegnarsi sopra alla freccia e coprirla parzialmente (MapLibre disegna i layer aggiunti più di recente sopra ai precedenti). Corretto invertendo l'ordine di aggiunta (tracciato, poi posizione/freccia, poi waypoint) sia alla creazione della mappa sia ad ogni aggiornamento.
3. **Mappe offline**: download dell'area del percorso caricato prima di partire, per garantire funzionamento senza copertura cellulare.
   - **Fattibilità "intera Italia" verificata e scartata (agosto 2026)**: chiesto se scaricare tutta la mappa italiana in locale fosse fattibile o troppo pesante. Distinzione chiave: l'estratto OSM grezzo (Geofabrik, `italy-latest.osm.pbf`) pesa 2,1 GB ma non è utilizzabile direttamente da MapLibre — è il dato sorgente, non le tile. Le tile vettoriali pronte (formato MBTiles) per un paese delle dimensioni dell'Italia, ai livelli di zoom utili in escursione, si stimano nell'ordine di 200-400 MB — spazio trascurabile su un telefono moderno. Il problema non è lo spazio ma il metodo: l'`OfflineManager` di MapLibre è pensato per un bounding box (un'area), non per scaricare un intero paese tile per tile dal server pubblico OpenFreeMap — farlo così rischierebbe migliaia di richieste e un blocco per uso anomalo (stesso tipo di problema anti-bot già visto con Overpass). Un pacchetto "Italia intera" pre-costruito e distribuito da noi sarebbe tecnicamente possibile ma aggiungerebbe un'infrastruttura di generazione/hosting/aggiornamento sproporzionata rispetto al bisogno reale.
   - **Decisione**: si resta sull'idea iniziale — download dell'area del percorso (bounding box con margine) prima di partire, via `OfflineManager` di MapLibre, senza pacchetti nazionali precostituiti.
   - **Non è specifico per l'Italia**: la fonte dati (OpenStreetMap) e lo stile (OpenFreeMap `liberty`) sono globali, non limitati a un paese, e l'`OfflineManager` scarica un'area in base a coordinate (bounding box), non in base a confini nazionali. La stessa funzionalità di download area-per-area funziona quindi ovunque nel mondo ci sia copertura OSM, senza alcuna modifica al codice: un percorso in Francia, Svizzera o altrove si scarica offline con la stessa identica logica di uno in Italia.
4. **Luoghi utili**: elenco di ristoranti, bar, trattorie, hotel, ostelli, rifugi nei dintorni del tracciato o della posizione corrente, con filtro per categoria (tutti / ristoranti / bar / trattorie / hotel / ostelli / rifugi), dati da OpenStreetMap (Overpass API).
5. **Gestione batteria**: indicatore di consumo stimato, modalità a basso consumo (riduzione frequenza GPS quando il movimento è lineare).

Criterio di uscita dalla fase: un utente non esperto riesce a caricare un percorso, seguirlo dall'inizio alla fine restando orientato nei punti critici, e trovare un posto dove mangiare o dormire lungo la strada, tutto offline.

## Fase 2 — Consolidamento (2-3 mesi dopo l'MVP)

- Cronologia dei percorsi seguiti, con statistiche (distanza, dislivello, tempo).
- Profilo altimetrico del tracciato.
- Guida vocale opzionale per i cambi di direzione.
- Cache locale dei luoghi utili (per rendere disponibile l'elenco POI anche offline dopo il primo download di un'area).
- Ricerca luoghi utili per raggio dalla posizione corrente, oltre che lungo il tracciato.

### Richieste utente da sviluppare — dopo aver testato adeguatamente la versione attuale

Annotate su richiesta esplicita (agosto 2026). Da affrontare in quest'ordine indicativo, dopo il test sul campo del flusso base (import GPX opzionale, navigazione, luoghi utili):

1. **Tracciamento del percorso effettuato** — [x] **implementato (v1.11)**: registrazione del cammino realmente fatto, indipendente da un percorso GPX caricato come guida (funziona sia seguendo una traccia sia partendo semplicemente con "Avvia registrazione"). Pulsanti Avvia/Pausa/Riprendi/Termina in `TrackingControls.kt`; motore di registrazione in `data/tracking/TrekRecorder.kt`; salvataggio in `data/tracking/ActivityStorage.kt`. Al termine salva tempo totale, tempo in movimento, distanza in km e dislivello.
   - **Pulsante di pausa**: [x] fatto — interrompe il conteggio di tempo/distanza durante una sosta, senza fermare la registrazione dell'intero percorso.
   - **Avviso automatico "sei ancora in pausa?"**: [x] fatto — se durante la pausa il GPS rileva uno spostamento oltre una soglia (30 m) rispetto al punto in cui è scattata la pausa, compare un avviso a schermo per ricordare di premere "Riprendi".
   - **Scelte deliberate per contenere il rischio di questo incremento** (da rivedere se servisse di più):
     - **Persistenza a file JSON, non Room**: Room richiede un nuovo plugin Gradle (KSP o kapt) per l'elaborazione delle annotazioni, non ancora presente nel progetto — rischio di un'altra incompatibilità di versioni da scoprire solo in CI, come già successo con AGP/Kotlin. Un file JSON nella cartella privata dell'app (`kotlinx.serialization`, già una dipendenza) basta per il volume di dati atteso. Room resta l'opzione se in futuro servissero query più ricche o il volume crescesse molto.
     - **Dislivello dal solo GPS, senza barometro**: mitigato con una soglia minima per segmento (3 m) per limitare il rumore verticale tipico del GPS (vedi "Spunti dalla ricerca competitiva" sopra), ma resta meno preciso di un approccio ibrido con barometro — miglioramento futuro, non incluso ora.
     - **"Tempo in movimento" approssimato**: oggi è "tempo totale meno le pause manuali", non un vero rilevamento automatico dei tratti fermi (es. una sosta per una foto senza premere "Pausa" viene comunque contata come tempo in movimento) — un affinamento possibile in futuro, se si rivelasse importante dopo l'uso reale.
   - **Conferma prima di "Termina"**: [x] **fatto (v1.12)** — prima il tap su "Termina" fermava la registrazione all'istante; ora compare sempre un dialog di conferma ("Terminare la registrazione?"), per evitare di interromperla per errore. Richiesto esplicitamente dopo il primo test sul campo del tracciamento.
   - **Conteggio passi**: [x] **fatto (v1.12)** — sensore hardware `Sensor.TYPE_STEP_COUNTER` letto tramite `SensorManager` in `MainMapScreen.kt`, con permesso `ACTIVITY_RECOGNITION` richiesto a runtime insieme a quello di localizzazione (solo su Android 10+, dove serve). I passi vengono inoltrati a `TrekRecorder.onStepCountSensorUpdate()`, che li conta solo mentre lo stato è RECORDING (esclusi durante la pausa, con lo stesso principio già usato per distanza e tempo in movimento — chi cammina durante una sosta segnata come pausa non vede quei passi aggiunti al totale). Scartata l'alternativa Health Connect/Google Fit: Google Fit è in dismissione entro fine 2026, Health Connect è pensata per sincronizzare dati tra più app salute, più pesante di quanto serva qui. Salvato in `CompletedActivity.stepCount` (nullable: `null` se il sensore non è disponibile sul dispositivo o il permesso non è stato concesso — mostrato come "Passi non disponibili", non un errore).
     - **Bug corretto (v1.16)**: gli utenti che avevano già concesso il permesso di localizzazione prima dell'introduzione del conteggio passi (v1.12) non vedevano mai richiesto `ACTIVITY_RECOGNITION`, perché nel codice la richiesta era annidata dentro il ramo `else` del controllo sul permesso di localizzazione — cioè veniva valutata solo quando la localizzazione **non** era ancora concessa. Risultato: passi sempre assenti (home page e fine percorso), senza alcun errore visibile. Corretto in `MainMapScreen.kt` disaccoppiando i due controlli: `ACTIVITY_RECOGNITION` viene ora richiesto ogni volta che manca, indipendentemente dallo stato del permesso di localizzazione.
   - Vedi punto 2 qui sotto per la schermata di Cronologia.
2. **Cronologia percorsi** — [x] **implementato (v1.12)**: schermata per rivedere l'elenco dei percorsi salvati dalla registrazione del punto 1. Due livelli:
   - **Elenco** (`ActivityHistoryScreen.kt`): data, distanza, tempo in movimento, dislivello e **conteggio passi** per ciascun percorso salvato, più recenti prima.
   - **Dettaglio** (`ActivityDetailScreen.kt`): aprendo un percorso dall'elenco, il tracciato disegnato sulla mappa (non solo i numeri) — riusando `TrekMapView.kt` in modalità "sola lettura" tramite il nuovo parametro `showCurrentPosition = false` (nasconde il cerchio di posizione corrente, che non avrebbe senso mescolato a un tracciato del passato; nessuna freccia di navigazione né avviso fuori percorso, che TrekMapView non disegna comunque). `CompletedActivity.points` (con timestamp) viene convertito al volo in `GpxTrack`/`TrackPoint` (senza timestamp) per riusare lo stesso layer di disegno già scritto per i file GPX. Scelta in linea con le app concorrenti viste nella ricerca (Wikiloc, Komoot, Gaia GPS, AllTrails), che mostrano tutte il percorso sulla mappa nella cronologia.
   - Punto d'ingresso: nuova icona "Cronologia" nella barra in alto di `MainMapScreen.kt`, accanto a "Luoghi utili". Il dettaglio riceve solo l'id del percorso come argomento di navigazione (`AppNavHost.kt`) e rilegge `ActivityStorage.loadAll()` per trovarlo, senza passare l'intero oggetto fra le schermate — coerente con l'uso già fatto altrove di un file JSON come unica fonte di verità.
   - **Cancellazione di un percorso salvato**: [x] **fatto (v1.13)** — pulsante di eliminazione (icona cestino) sia nell'elenco (`ActivityHistoryScreen.kt`) sia nel dettaglio (`ActivityDetailScreen.kt`), sempre con conferma prima di cancellare davvero (stesso principio già applicato a "Termina"). `ActivityStorage.delete(context, activityId)` rilegge, filtra via l'id, riscrive il file. Dal dettaglio, la cancellazione elimina anche gli eventuali file foto associati ai waypoint del percorso (`PhotoStorage.delete`), per non lasciare file orfani nella cartella privata dell'app.
3. **Foto geolocalizzate sul percorso** — [x] **implementato (v1.13)**: possibilità di scattare una foto dall'interno dell'app durante il cammino (pulsante "Foto" in `TrackingControls.kt`, visibile durante RECORDING/PAUSED); la foto viene associata al punto esatto in cui è stata scattata e il percorso salvato mostra un'indicazione visiva sulla mappa (cerchio arancione, layer dedicato in `TrekMapView.kt`) nei punti in cui sono state scattate una o più foto/scritte note (vedi punto 4).
   - **Scelta tecnica**: scatto delegato all'app Fotocamera di sistema tramite intent (`ACTION_IMAGE_CAPTURE`/`ActivityResultContracts.TakePicture()`), non un'API Camera diretta in-app — evita di dover richiedere il permesso `CAMERA` e di scrivere/mantenere una UI di scatto, stessa filosofia di semplicità già usata per `Intent.ACTION_DIAL` sui luoghi utili. Serve però un `FileProvider` (dichiarato in `AndroidManifest.xml` + `res/xml/file_paths.xml`) per dare alla fotocamera un file scrivibile nella cartella privata dell'app (`data/tracking/PhotoStorage.kt`), altrimenti si otterrebbe solo un'anteprima a bassa risoluzione.
   - **Anteprime**: le foto vengono mostrate in miniatura nel dettaglio del percorso con downscaling (`PhotoStorage.loadThumbnail`, `inSampleSize` calcolato sulle dimensioni reali) per non rischiare di esaurire la memoria disponibile caricando più foto a piena risoluzione in una lista.
   - **Non incluso in questa versione**: nessuna icona per tipo di punto (foto vs nota — vedi "Icone per categoria sui punti" negli Spunti dalla ricerca competitiva) e nessun tocco sul marker della mappa per aprire direttamente la foto/nota corrispondente; il dettaglio si legge nell'elenco sotto la mappa, non sulla mappa stessa. Miglioramento possibile in futuro se si rivelasse utile dopo l'uso reale.
4. **Due tipi di note** — [x] **implementato (v1.13)**, stesso modello dati delle foto (vedi sotto):
   - **Nota generale del percorso**: campo di testo modificabile nel dettaglio del percorso (`ActivityDetailScreen.kt`), con pulsante "Salva nota" — `CompletedActivity.generalNote`, aggiornabile anche giorni dopo la registrazione (non solo al momento di "Termina"), tramite la nuova `ActivityStorage.update()`.
   - **Nota puntuale geolocalizzata**: pulsante "Nota" in `TrackingControls.kt` (visibile durante RECORDING/PAUSED, come "Foto"), apre un piccolo dialog con un campo di testo; salvata con la posizione corrente e mostrata sulla mappa nel dettaglio insieme alle foto.
   - **Modello dati unico per foto e note** (`ActivityWaypoint` in `data/tracking/TrackingModels.kt`): un solo `id`/`latitude`/`longitude`/`timestampMillis`, con `note` e/o `photoFileName` opzionali — scelta in linea con Gaia GPS (vedi "Spunti dalla ricerca competitiva" sotto), che tratta una foto come una nota puntuale con immagine allegata invece di due sistemi paralleli. `TrekRecorder` le accumula in memoria durante la registrazione (`addNoteWaypoint`/`addPhotoWaypoint`) e le include in `CompletedActivity.waypoints` solo al termine.
   - **Disponibili sia durante la registrazione attiva sia in pausa**: aggiungere una nota o una foto mentre ci si è fermati (es. per il panorama) è un caso d'uso legittimo, a differenza di distanza/tempo/passi che restano fermi in pausa.
5. **Scoperta cammini nelle vicinanze, con download GPX** — [x] **implementato (v1.16)**: dalla posizione corrente, elenco di sentieri trovati nei dintorni (relazioni `route=hiking` su OpenStreetMap, raggio 5 km), con distanza dall'utente, lunghezza, tempo stimato e difficoltà quando nota. Da qui si può usare direttamente un sentiero come percorso guida, oppure scaricarne una copia come vero file `.gpx`.
   - **Query Overpass** (`data/trails/TrailQueryBuilder.kt`): `relation(around:R,lat,lon)["route"="hiking"]; out tags; way(r); out geom;` — `way(r)` recupera le way membro della relazione, `out geom` include la geometria (lat/lon di ogni punto) di ciascuna way nella stessa risposta, senza bisogno di una terza query per i nodi. Riusa lo stesso client Overpass già configurato per i luoghi utili (`data/poi/OverpassApiService.kt`: mirror, timeout, header), cambia solo query e modello di risposta (`TrailOverpassModels.kt`).
   - **Ricostruzione del tracciato** (`TrailRepository.stitchWays`): le way membro di una relazione OSM sono elencate in ordine, ma la geometria di ciascuna può essere digitata indifferentemente in un verso o nell'altro, senza garanzia di coerenza tra way consecutive. Per ogni way dopo la prima si sceglie l'orientamento (dritto o invertito) che continua più vicino all'ultimo punto già aggiunto, evitando un tracciato "a zig-zag" e una lunghezza calcolata gonfiata.
   - **Tempo stimato, senza dislivello**: come già previsto come limite in fase di fattibilità, `out geom` restituisce solo lat/lon, non l'altitudine — una stima alla Naismith (che richiede il dislivello) non è quindi possibile con questi dati. Si usa invece una velocità media costante (4 km/h, `NearbyTrail.estimatedMinutes()`), con nota esplicita a schermo che si tratta di una stima approssimata, senza tener conto della pendenza — semplificazione dichiarata, non un valore camuffato da preciso.
   - **Difficoltà** (`TrailQueryBuilder.difficultyFor`): letta dal tag `sac_scale` quando presente (7 livelli OSM); se assente o non riconosciuto, mostrato "difficoltà non specificata" — mai un valore inventato, stesso principio già applicato altrove nel progetto per dati OSM a copertura non uniforme.
   - **Numero ufficiale del sentiero (tag `ref`)** — [x] **fatto (v1.19)**: richiesto esplicitamente di mostrare, quando disponibile, il numero ufficiale del sentiero (es. numerazione CAI tipo "126") oltre al nome — il codice riportato sulla segnaletica reale sul terreno, spesso più utile del nome descrittivo per riconoscere un sentiero durante il cammino. `NearbyTrail.ref` (tag OSM `ref`, nullable: copertura non uniforme, stesso limite già noto per `sac_scale`) e `NearbyTrail.displayName()` (`"numero · nome"` quando `ref` è presente, altrimenti solo il nome) in `data/trails/TrailModels.kt`, usato sia nell'elenco (`NearbyTrailsScreen.kt`) sia come nome del percorso una volta scelto come guida (`toGpxTrack()`, quindi compare anche nel nuovo titolo "Stai seguendo: ..." qui sopra).
   - **Download GPX** (`data/gpx/GpxWriter.kt`): genera un file GPX 1.1 valido dal tracciato ricostruito, salvato tramite il selettore file di sistema (`ActivityResultContracts.CreateDocument`), così l'utente ottiene un vero file da tenere o riaprire altrove.
   - **Uso diretto come percorso guida**: un tocco imposta il sentiero scelto come `CurrentTrackHolder.track` (sostituisce l'eventuale percorso già caricato) e torna alla mappa — nessuna conferma richiesta, stessa scelta già fatta per "Cambia percorso" in `MainMapScreen.kt` (azione diretta e reversibile, non distruttiva).
   - **Punto d'ingresso**: nuova icona "Sentieri vicini" nella barra in alto di `MainMapScreen.kt`, accanto a Cronologia/Luoghi utili/Emergenza.
   - **Alternativa valutata e non adottata**: Waymarked Trails (`hiking.waymarkedtrails.org/api/v1`) avrebbe potuto semplificare la query rispetto a Overpass scritto da zero, ma si è preferito restare su un'unica fonte/infrastruttura dati (Overpass) già in uso e verificata nel resto dell'app, per non aggiungere una dipendenza da un secondo servizio terzo senza un beneficio chiaro.
6. **Luoghi utili: distanza, navigazione e telefono cliccabile** — [x] **implementato (v1.15)**: `PlacesScreen.kt` mostra ora la distanza dalla posizione corrente accanto a ogni luogo (riusando `NavigationEngine.distanceMeters`, la stessa formula già scritta per i tracciati GPX) e il numero di telefono, quando presente, cliccabile per aprire il tastierino (`Intent.ACTION_DIAL`). Toccando un luogo si avvia la navigazione verso di lì.
   - **Navigazione verso un luogo, senza duplicare NavigationEngine**: `PoiNavigationHolder` (nuovo, `data/navigation/`) tiene solo il luogo scelto (nome + coordinate), separato da `CurrentTrackHolder` che tiene il percorso GPX. `MainMapScreen.kt` costruisce al volo un `GpxTrack` sintetico di due punti — **la posizione corrente stessa** (aggiornata ad ogni fix GPS) e il luogo scelto — e lo passa a `NavigationEngine`, riusando così freccia direzionale e distanza già scritte per i tracciati GPX senza modificarle. Effetto collaterale voluto: il punto più vicino del "percorso" è sempre quello di partenza (distanza 0), quindi l'avviso "fuori percorso" (pensato per un vero tracciato da seguire) non scatta mai in questa modalità — corretto, dato che si tratta di andare in linea d'aria verso un punto, non di seguire un sentiero tracciato.
   - **Perché non passa dal layer "track" della mappa**: il percorso sintetico cambia ad ogni aggiornamento GPS (il primo punto è sempre "dove sono ora"). Se venisse passato a `TrekMapView` come tracciato da disegnare, la mappa "salterebbe" continuamente per re-inquadrare la camera sul nuovo tracciato ad ogni fix. Si mostra invece solo un marker (riusando il layer `waypoints` già scritto per foto/note) nel punto di destinazione — l'utente vede comunque dove deve arrivare, senza il problema del salto della camera.
   - **Avviso all'avvio della navigazione verso un luogo utile**: [x] fatto — se un percorso GPX è già caricato, prima di avviare la navigazione verso un luogo compare un avviso ("Stai lasciando il percorso"), con conferma esplicita. Il percorso GPX non viene mai toccato/perso: terminando la navigazione verso il luogo (pulsante dedicato, sostituisce i pulsanti "Cambia percorso"/"Rimuovi percorso" mentre attiva) la navigazione sul percorso GPX originale riprende da sola, perché `CurrentTrackHolder` non è mai stato modificato.
7. **Pulsante Emergenza** — [x] **implementato (v1.15)**: invio rapido di una richiesta d'aiuto con le coordinate GPS a numeri preimpostati, via SMS (automatico) e WhatsApp (predisposto, invio manuale per contatto).
   - **Due schermate dedicate**: [x] fatto — `EmergencyScreen.kt` ("Emergenza": posizione, numeri di emergenza locali, pulsante di invio SOS, elenco contatti con scorciatoie chiamata/WhatsApp) e "Impostazioni" (aggiungi/modifica/elimina contatti, persistenza JSON in `data/emergency/EmergencyContactsStorage.kt` — stesso pattern di `ActivityStorage.kt`; da v1.20 è `ui/screens/settings/SettingsScreen.kt`, non più `EmergencySettingsScreen.kt` — vedi punto "Impostazioni generale" più sotto). Entrambe raggiungibili dalla mappa principale (icona dedicata in `MainMapScreen.kt`) e non da un pulsante diretto nella schermata principale, come richiesto.
   - **SMS**: [x] fatto — `EmergencyMessenger.sendSms` invia a tutti i contatti configurati un messaggio con coordinate e link Google Maps, tramite `SmsManager` (permesso `SEND_SMS`, richiesto a runtime al primo utilizzo). Messaggio diviso automaticamente in più parti se supera la lunghezza di un SMS singolo (`divideMessage`/`sendMultipartTextMessage`), per non rischiare un errore su messaggi con indirizzo lungo.
   - **WhatsApp**: [x] fatto — `EmergencyMessenger.whatsAppIntent` apre WhatsApp con lo stesso messaggio già scritto verso un contatto, un tocco per contatto nell'elenco di `EmergencyScreen.kt` (l'invio finale resta sempre dell'utente, come previsto).
   - **Numeri di emergenza locali, rilevati offline** — [x] fatto, **con una semplificazione dichiarata**: `data/emergency/EmergencyCountryLookup.kt` mostra sempre il 112 più i numeri locali supplementari (polizia/ambulanza/vigili del fuoco quando diversi dal 112) per una ventina di paesi europei. Il piano originale ipotizzava un confronto punto-poligono con i confini nazionali reali; non è stato possibile scaricare un dataset di confini offline in questo ambiente di sviluppo (nessun accesso di rete a GitHub/CDN dal sandbox usato per scrivere il codice — verificato con più tentativi), quindi si usa un rettangolo (bounding box) approssimativo per paese invece del confine reale, con l'euristica "vince il rettangolo più piccolo" nelle zone di sovrapposizione (i paesi piccoli citati hanno un'area molto minore dei vicini più grandi, quindi l'euristica funziona bene in pratica). Vicino a un confine il paese rilevato può sbagliare, ma il 112 resta sempre il numero principale mostrato e funziona ovunque in Europa a prescindere — impatto pratico limitato. Se in futuro sarà disponibile un dataset di confini reali da includere come risorsa dell'app, `EmergencyCountryLookup` è il solo file da sostituire, la sua interfaccia pubblica non cambierebbe. Numeri verificati (agosto 2026) da fonti pubbliche sui numeri di emergenza europei.
   - **Coordinate e indirizzo in pagina "Emergenza"**: [x] fatto — le coordinate GPS (da `LocationTrackingService`, lo stesso servizio già usato dalla mappa principale) sono sempre visibili; l'indirizzo/toponimo (`data/emergency/ReverseGeocoder.kt`, `android.location.Geocoder`) è un'informazione aggiuntiva, mostrata solo se disponibile — richiede tipicamente una connessione dati, quindi spesso assente in montagna: gestito esplicitamente come caso normale ("indirizzo non disponibile"), non come errore.
   - **Fattibilità (agosto 2026)**: confermata per l'SMS (l'app non passa dallo store di Google Play, quindi non si applicano le limitazioni sui permessi SMS previste per le app pubblicate lì). Per zone davvero senza copertura cellulare, il vero standard resta un dispositivo satellitare dedicato (Garmin inReach, Spot, ZOLEO) — fuori dallo scopo di un'app per smartphone, limite onesto della funzionalità, non promesso come sostituto.
8. **Check globale dell'app** (richiesto esplicitamente, agosto 2026): revisione completa del codice per individuare criticità di programmazione o di sicurezza (es. gestione errori mancante, permessi richiesti in eccesso, dati sensibili loggati o esposti) e parti di programma inserite ma non più utilizzate (import, funzioni, file "morti" rimasti da iterazioni precedenti), oltre a possibili ottimizzazioni. Da fare come passata trasversale su tutto il codice esistente, non su una singola funzionalità — collocata per ultima in coda perché ha più senso farla dopo aver completato gli sviluppi dei punti precedenti, per non doverla ripetere ad ogni aggiunta.
9. **Attribuzione OpenStreetMap** (richiesto esplicitamente, agosto 2026 — "regolarizzare l'uso" dei dati OSM prima di distribuire l'app più ampiamente): la licenza dei dati OSM (ODbL) richiede che l'utente sia reso ragionevolmente consapevole che i dati vengono da OpenStreetMap, con testo "© OpenStreetMap contributors" e un riferimento a openstreetmap.org/copyright. Da fare:
   - **Mappa**: probabilmente già a posto — MapLibre mostra di default un pulsante "i" con le attribuzioni (non disattivato nel codice) — ma da verificare a video che non sia coperto dal pulsante "Ricentra", entrambi in basso a destra.
   - **Luoghi utili**: manca del tutto. Aggiungere una scritta tipo "Dati luoghi: © OpenStreetMap contributors" in fondo a `PlacesScreen.kt`, cliccabile verso openstreetmap.org/copyright.
   - **Chiarimento sui limiti d'uso di Overpass API** (separato dalla licenza dati — è la policy del servizio gratuito, non un vincolo legale sui dati): il limite di circa 10.000 richieste/giorno e 1 GB/giorno è applicato **per indirizzo IP**, non per app né in modo aggregato su tutte le installazioni — ogni utente ha quindi la propria "quota" indipendente, e più installazioni dell'app di per sé non avvicinano a nessun tetto condiviso. Il rischio reale è più sottile: utenti dietro lo stesso IP pubblico (es. NAT condiviso di un operatore mobile, comune per il traffico dati cellulare, o un rifugio/wifi condiviso con altri utilizzatori di Overpass) potrebbero "consumare" insieme la quota di quell'IP, indipendentemente da questa app. Superato il limite, il server risponde con **HTTP 429 Too Many Requests** (non un ban permanente, un blocco temporaneo) — già visibile in app grazie al dettaglio tecnico mostrato in "Luoghi utili" dalla v1.6, stesso meccanismo che ha già permesso di diagnosticare gli errori 406/504 passati. Non serve nessuna modifica per gestirlo *ora*: alla scala di un'app con poche/moderate installazioni è molto improbabile arrivarci con l'uso normale (si interroga Overpass solo quando l'utente apre "Luoghi utili", non in continuo). Se in futuro l'app crescesse molto, la soluzione strutturale resta quella già annotata in "Rischi da monitorare": un mirror Overpass auto-ospitato o una copia propria dei dati, invece del server pubblico condiviso.
10. **Schermata iniziale con branding GM Consulenze** (richiesto esplicitamente, agosto 2026): prima schermata mostrata all'apertura dell'app, prima della mappa. Contenuto: logo GM Consulenze, link al sito, email di contatto, breve descrizione di cosa fa l'app, elenco delle tecnologie/licenze usate (qui in forma riassuntiva — menzione di OpenStreetMap/ODbL oltre alle attribuzioni già previste al punto 9 nelle schermate dove i dati vengono effettivamente usati), indicazione che l'app è completamente gratuita, e un pulsante ben visibile "Passa alla navigazione" per entrare nell'app vera e propria. Obiettivo esplicito: incuriosire verso GM Consulenze senza essere invasiva.
    - **Sito GM Consulenze**: [https://www.gmconsulenze.it](https://www.gmconsulenze.it) — confermato (agosto 2026).
    - **Email di contatto**: non ancora decisa — da inserire quando comunicata.
11. **Informativa privacy** (richiesto esplicitamente, agosto 2026 — chiarimento su cosa serve, non ancora implementato). Non è avvocato chi scrive, quanto segue è un'indicazione pratica, non una consulenza legale formale.
    - **Serve?** Sì, probabilmente — il GDPR si applica a prescindere dal canale di distribuzione (vale uguale su GitHub/Aptoide come sul Play Store), e l'app tratta dati che richiedono comunque trasparenza verso l'utente: posizione GPS (sempre), foto (se l'utente le scatta), eventualmente numeri di telefono di terzi per l'Emergenza (punto 7). Anche gli store di terze parti, in pratica, spesso chiedono un link alla privacy policy in fase di pubblicazione.
    - **Cosa fanno le concorrenti**: Komoot mostra un vero banner di consenso ("Accetta tutti i cookie" + preferenze personalizzabili) perché usa SDK di terze parti per pubblicità/statistiche/personalizzazione — un caso diverso dal nostro. Non risultano dettagli pubblici equivalenti per AllTrails, Wikiloc, Gaia GPS dalla ricerca fatta.
    - **La differenza importante per GM-Trekking**: un banner "accetta per continuare" serve quando il trattamento dati non è già di per sé giustificato dalla funzione richiesta dall'utente (tipicamente pubblicità/tracciamento/analisi) — questa app non ha SDK pubblicitari, non ha analytics, non ha account utente, nessun dato viene venduto o condiviso per marketing. Quello che serve davvero è **trasparenza informativa** (spiegare cosa viene raccolto e perché), non un consenso bloccante da far accettare per usare l'app.
    - **Cosa dichiarare, in pratica**: posizione GPS (necessaria per navigazione/luoghi utili, mai inviata a noi, solo alla mappa OpenFreeMap e a Overpass API — entrambi servizi tecnici di terze parti, non tracciamento pubblicitario); foto e note (scritte/scattate solo su azione esplicita dell'utente, salvate solo in locale sul telefono); numeri di telefono d'emergenza (punto 7, salvati solo in locale, usati solo per l'invio esplicito richiesto dall'utente); nessun account, nessuna raccolta per finalità di marketing. Nota onesta da includere: il backup automatico di Android (`android:allowBackup="true"`, già nel manifest) potrebbe copiare i dati dell'app sul account Google dell'utente se il backup di sistema è attivo — non è condivisione con noi o terzi, ma va detto con chiarezza.
    - **Dove metterla**: probabilmente una sezione dedicata nella futura schermata "Informazioni" (raggiungibile dalla mappa principale, stesso punto d'ingresso già previsto al punto 10), non un passaggio obbligato da accettare prima di usare l'app.
    - **Quando mostrarla — deciso (agosto 2026)**: al primo avvio dopo l'installazione, e di nuovo al primo avvio dopo ogni aggiornamento (non ad ogni apertura) — più una voce "Informazioni" sempre raggiungibile dalla mappa principale per rivederla quando si vuole. Implementazione prevista: salvare in `SharedPreferences` l'ultimo `versionCode` (da `BuildConfig.VERSION_CODE`) per cui la schermata è già stata mostrata; se il valore salvato è diverso da quello corrente (compreso il caso "mai salvato", cioè primissima apertura), mostrarla prima di arrivare a `MainMapScreen`, poi aggiornare il valore salvato. La voce "Informazioni" nella mappa principale la riapre sempre, a prescindere da questo controllo.
12. **Indicazione "adatto ai cani" sui luoghi utili e/o sui percorsi** (idea proposta, agosto 2026 — verificata la fattibilità, non ancora iniziata). **Il tag esiste davvero su OpenStreetMap**: `dog=yes` / `dog=no` / `dog=leashed` (solo al guinzaglio) / `dog=unleashed` (libero), usabile su sentieri (`highway=path/footway`), parchi, aree naturali e locali (bar/ristoranti) — [documentazione ufficiale](https://wiki.openstreetmap.org/wiki/Key:dog), stato "de facto" (convenzione consolidata della community, non uno standard formale, ma ampiamente riconosciuta e già usata da migliaia di elementi mappati nel mondo).
    - **Limite reale da tenere presente**: è pensato soprattutto come **restrizione**, non come conferma positiva — la sua stessa documentazione dice che `dog=yes` di norma non serve nemmeno scriverlo su un sentiero generico, perché è già il comportamento predefinito quando i cani non sono esplicitamente vietati (`foot=yes` implica già "cani ammessi, secondo le leggi locali sul guinzaglio"). Il tag compare quindi soprattutto dove c'è un divieto o un obbligo di guinzaglio da segnalare (parchi, riserve naturali, alcuni locali), molto meno sui sentieri di montagna "normali" — che sono proprio il caso d'uso principale di GM-Trekking. Non è stato possibile verificare la copertura reale in Italia con numeri precisi in questo ambiente di sviluppo (nessun accesso di rete a taginfo.openstreetmap.org dal sandbox usato per scrivere il codice), ma per lo stesso motivo già visto con `sac_scale` (copertura non uniforme, vedi punto 5) è ragionevole aspettarsi che la maggior parte dei sentieri semplicemente non abbia questo dato, non che sia "non adatto ai cani".
    - **Conclusione**: tecnicamente fattibile, stessa strada già scelta per orari/telefono/categoria dei luoghi utili — aggiungere `dog` alla query Overpass e mostrare l'informazione quando presente (`Poi.dogPolicy: String?` o simile), con un'icona e testo tipo "cani ammessi al guinzaglio" solo se il dato esiste, e un caso onesto "non specificato" (non "non ammessi") quando manca, esattamente come già fatto per `sac_scale` nel punto 5. Da presentare all'utente come un'informazione aggiuntiva quando disponibile, non come garanzia di copertura completa — altrimenti si rischia di promettere più di quanto i dati permettano davvero.
    - **Non ancora iniziato**: in attesa di conferma per procedere.
13. **Impostazioni generale, chiave API per l'instradamento reale** — [x] **implementato (v1.20)**: richiesto esplicitamente di trasformare la vecchia pagina "Impostazioni" (che conteneva solo i contatti d'emergenza, `EmergencySettingsScreen.kt`) in una pagina generale con sezioni titolate, contenitore di tutte le opzioni configurabili presenti e future, pensando fin da subito a una futura distribuzione a molti utenti — non solo all'uso attuale ristretto.
    - **Nuova `ui/screens/settings/SettingsScreen.kt`**, sostituisce `EmergencySettingsScreen.kt` (rimosso): due sezioni, ciascuna con titolo e breve descrizione di cosa si configura. "Contatti di emergenza" (stesso contenuto/logica di prima, invariato) e "Navigazione" (nuova, chiave API di OpenRouteService — vedi punto 2 di questa fase, "Instradamento reale"). Raggiungibile sia da una nuova icona dedicata nella mappa principale (`MainMapScreen.kt`, accanto a Cronologia/Luoghi utili/Sentieri/Emergenza — deve essere facile da trovare, non nascosta dentro un'altra schermata, coerente con "presenti e future") sia dalla scorciatoia già esistente nella schermata "Emergenza".
    - **Bug segnalato e corretto — "dopo aver inserito un contatto non riesco più ad aggiungerne altri"**: la revisione del codice della vecchia schermata non ha individuato un errore di logica netto nel flusso di aggiunta (senza accesso a Logcat non è stato possibile riprodurlo con certezza), ma il pattern usato — due variabili di stato separate (`editingContact`/`showAddDialog`) per governare lo stesso dialog — lasciava aperta la possibilità di un disallineamento tra le due. Riscritto con un solo stato (`ContactDialogState`: Hidden/Adding/Editing), che esclude per costruzione questa possibilità; sostituito anche il pulsante flottante "+" con un pulsante "Aggiungi contatto" esplicito sotto l'elenco (più chiaro ora che la pagina ha più sezioni, un FAB unico sarebbe stato ambiguo su quale sezione riguardasse), e l'id generato per un nuovo contatto è passato da un timestamp (`System.currentTimeMillis()`, teoricamente soggetto a collisioni se due contatti vengono aggiunti nello stesso millisecondo) a un UUID (nessuna possibilità di collisione).
    - **Nuova `data/settings/AppSettingsStorage.kt`**: `SharedPreferences` invece del pattern a file JSON usato altrove (`EmergencyContactsStorage.kt`, `ActivityStorage.kt`) — scelta deliberata, perché qui si tratta di poche coppie chiave/valore scalari (per ora solo la chiave API), il caso d'uso esatto per cui `SharedPreferences` esiste nella piattaforma Android, più semplice di un file JSON per un singolo valore. Pensata come contenitore generale per le impostazioni scalari future, non solo per questa chiave.
    - **Solo configurazione in v1.20, instradamento vero implementato in v1.24**: v1.20 salvava solo la chiave inserita dall'utente, senza fare ancora nessuna chiamata al servizio. La sezione include un link diretto alla pagina di registrazione gratuita di OpenRouteService (`openrouteservice.org/sign-up`), una spiegazione del perché serve registrarsi (gratuito, ma con un limite di richieste giornaliere oltre il quale il servizio si interrompe temporaneamente), e una nota sulla privacy aggiornata in v1.20/v1.24 al tempo presente (non più "quando sarà attiva"): ad ogni richiesta di instradamento (solo quando l'utente avvia la navigazione verso un luogo, non in continuo) la posizione e la destinazione scelta vengono inviate a OpenRouteService (terze parti).
    - **Dominio API cambiato**: segnalato dall'utente (ricevuto in fase di registrazione, agosto 2026) e confermato sull'annuncio ufficiale del team OpenRouteService (28 aprile 2026): il dominio `api.openrouteservice.org` è deprecato in favore di `api.heigit.org`, con **spegnimento definitivo del vecchio dominio previsto per il 24 agosto 2026**. La chiave API generata resta valida su entrambi i domini, non serve rigenerarla. Endpoint Directions usato: `https://api.heigit.org/openrouteservice/v2/directions/{profile}/geojson` (profilo `foot-hiking` per l'escursionismo) — **non** `api.openrouteservice.org/v2/directions/...`. Attenzione allo slash finale nel base URL passato a Retrofit: un `/` in più dopo `openrouteservice` nel base URL causa errori HTTP 405 "Method Not Allowed" (segnalato da altri sviluppatori nel forum ufficiale durante la stessa migrazione) — `RoutingApiService.kt` usa `BASE_URL = "https://api.heigit.org/openrouteservice/"` (senza slash extra) con il path `v2/directions/{profile}/geojson`.
    - **Bug reale trovato e risolto (v1.34) — HTTP 406 sulla chiamata di instradamento**: dopo aver ripristinato la chiave API (persa in un'installazione precedente) e riprovato la navigazione verso un luogo, il nuovo dettaglio tecnico introdotto in v1.28 ha mostrato subito la causa: "Percorso reale non disponibile... Dettaglio tecnico: HTTP 406" — esattamente lo stesso problema già affrontato per Overpass API (v1.6): l'header `Accept: application/json` di `RoutingApiService.kt` non includeva `application/geo+json` (il content-type reale restituito dall'endpoint `/geojson`), quindi il server rifiutava la richiesta come non soddisfacibile. Confermato dall'esempio ufficiale della documentazione OpenRouteService (curl), che usa `Accept: application/json, application/geo+json, application/gpx+xml, img/png; charset=utf-8`. Corretto replicando esattamente quell'header, più uno User-Agent descrittivo (stesso pattern già in `OverpassApiService.kt`). Prova pratica di quanto valga il dettaglio tecnico aggiunto in v1.28: senza quel testo a schermo, questo bug sarebbe rimasto un altro "non succede nulla" da indagare alla cieca.
    - **Instradamento reale implementato** — [x] **fatto (v1.24)**, su richiesta esplicita dell'utente ("procedi con l'implementazione del calcolo del percorso") dopo aver verificato che le rotte salvate sopravvivono a disinstallazione/reinstallazione (backup automatico Android, vedi punto 11). Nuovo pacchetto `data/routing/`: `RoutingModels.kt` (DTO per richiesta/risposta GeoJSON), `RoutingApiService.kt` (client Retrofit verso `api.heigit.org`, richiesta `POST` con corpo JSON `{"coordinates": [[lon,lat],[lon,lat]]}` — attenzione, longitudine PRIMA della latitudine, opposto alla convenzione lat/lon usata nel resto del progetto — risposta letta come stringa grezza con `ScalarsConverterFactory`, stesso pattern di `OverpassApiService.kt`, e parsata a mano con kotlinx.serialization), `RoutingRepository.kt` (`findHikingRoute(...)`, sempre avvolto in `runCatching`, ritorna `null` — mai un'eccezione al chiamante — per qualunque fallimento: chiave mancante, rete assente, quota esaurita, risposta malformata o percorso con meno di 2 punti).
      - **Nessuna nuova dipendenza Gradle necessaria**: Retrofit, `converter-scalars`, OkHttp e `kotlinx-serialization-json` erano già presenti nel progetto per `OverpassApiService.kt`, riusati identici per il client di instradamento.
      - **Degrado controllato, principio guida**: il percorso reale è un arricchimento "best effort", mai un blocco. Se manca la chiave API, o la richiesta fallisce per qualunque motivo (compreso il limite giornaliero di 2000 richieste dell'account gratuito), la navigazione continua comunque con la linea retta sintetica già usata da v1.15 — nessun errore mostrato all'utente, nessuna interruzione del flusso.
      - **`PoiNavigationHolder` con `CoroutineScope` proprio, non legato a una schermata Compose**: la richiesta di instradamento parte da `PlacesScreen.kt` (`startNavigation`), ma l'utente torna subito alla mappa principale (`onBack()`), che smonta la composizione di `PlacesScreen` — uno scope preso con `rememberCoroutineScope()` sarebbe stato annullato prima che la richiesta di rete avesse il tempo di completare. `PoiNavigationHolder` usa quindi un proprio `CoroutineScope(SupervisorJob() + Dispatchers.IO)`, indipendente da qualunque ciclo di vita di schermata: primo uso di questo pattern nel progetto (finora ogni lavoro asincrono usava lo scope di una composable). La destinazione (`Target`) viene impostata subito con `routePoints = null` (navigazione avviata immediatamente con la linea retta), poi aggiornata in background se/quando il percorso reale arriva.
      - **Linea disegnata sulla mappa, novità rispetto a v1.15**: prima, la navigazione verso un luogo utile mostrava solo un marker di destinazione, mai una linea — perché il "percorso" sintetico usato dal motore di navigazione cambia ad ogni fix GPS (il primo punto è sempre "dove sono ora"), e disegnarlo avrebbe fatto "saltare" continuamente la camera. Con un percorso reale ora disponibile (stabile, non cambia ad ogni fix), `MainMapScreen.kt` distingue due variabili: `navigationTrack` (usato dal motore `NavigationEngine` per frecce/distanze — usa il percorso reale se disponibile, altrimenti la linea retta come prima) e il nuovo `mapTrack` (usato solo per disegnare la linea sulla mappa tramite `TrekMapView` — mostra il percorso GPX caricato se presente, altrimenti il percorso reale della navigazione verso un luogo se disponibile, ma **mai** la linea retta sintetica, per non reintrodurre il problema del salto della camera).

14. **Backup dei dati salvati (percorsi, foto, note, contatti)** — richiesto esplicitamente (agosto 2026), non ancora iniziato. Segnalazione che ha portato al punto: i percorsi registrati sono sopravvissuti a una disinstallazione/reinstallazione completa dell'app, quasi certamente grazie al backup automatico di Android (`android:allowBackup="true"`, già nel manifest — copia i dati privati dell'app sull'account Google del telefono quando il backup di sistema è attivo). Questo però non è un vero backup dei dati dell'app: è un effetto collaterale di un'impostazione di sistema, non controllato né garantito da noi — dipende dalle impostazioni di backup dell'utente, non funziona se il backup di sistema è disattivato o se l'utente cambia telefono senza passare dal ripristino Google standard, e non copre alcun caso in cui vogliamo un backup esplicito e affidabile (es. esportazione manuale, cambio dispositivo controllato, protezione da cancellazione accidentale). Motivazione: se l'app verrà usata più a lungo o da più utenti, la cronologia dei percorsi (tempo, distanza, dislivello, passi, foto, note) diventa un dato a cui l'utente tiene, non solo un file tecnico — perderlo per una disinstallazione, un cambio telefono o un problema del dispositivo sarebbe un danno reale.
    - **Punto di partenza per la progettazione, quando si affronterà**: valutare un'esportazione/importazione esplicita in un unico file (coerente con l'export GPX già esistente per i singoli percorsi, punto 5), da salvare dove l'utente preferisce (stesso selettore file di sistema già usato per il download GPX) — indipendente dalle impostazioni di backup di Android, sotto il controllo diretto dell'utente. Un backup automatico/cloud vero e proprio (es. Google Drive) è un'alternativa più comoda ma più complessa (richiede autenticazione, quota, gestione dei conflitti) — da valutare solo se l'esportazione manuale si rivelasse insufficiente dopo l'uso reale, stesso approccio "per gradi" già seguito per l'instradamento reale (punto 13).
    - **Dati coinvolti**: `data/tracking/ActivityStorage.kt` (percorsi + foto + note), `data/emergency/EmergencyContactsStorage.kt` (contatti), `data/settings/AppSettingsStorage.kt` (chiave API e futuri parametri) — tutti già su file/SharedPreferences nella cartella privata dell'app, quindi leggibili e serializzabili senza cambiare il modello dati esistente.
15. **Segnalato su dispositivo reale (v1.24) — nessuna indicazione visibile sulla mappa navigando verso un luogo utile — RISOLTO (v1.28)**: due cause distinte, entrambe confermate e corrette.
    - **Diagnosi con l'utente**: la chiave API era già configurata e verificata funzionante esternamente (test diretto sul "API Playground" ufficiale di OpenRouteService — `openrouteservice.org/dev/#/api-docs/directions_service` — risposta corretta, struttura GeoJSON identica a quella attesa dai modelli dell'app in `RoutingModels.kt`). Non essendo riproducibile né osservabile da qui (niente Logcat, e il dominio `api.heigit.org` è bloccato dalla policy di rete dell'ambiente usato per scrivere questo codice, verificato con un tentativo diretto), la causa esatta lato app non era determinabile per esclusione.
    - **Causa 1 — nessuna diagnostica visibile, corretta**: `RoutingRepository.findHikingRoute` restituiva prima solo `List<TrackPoint>?` (`null` per qualunque fallimento, inghiottito da `runCatching`), quindi non c'era alcun modo di distinguere "chiave mancante" da "richiesta fallita" da "ancora in corso" — lo stesso problema già risolto per Overpass API con il "Dettaglio tecnico" (v1.6). Corretto: `findHikingRoute` ora restituisce `RoutingOutcome` (`Success`/`Failure(detail)`, nuovo file, stesso `RoutingRepository.kt`), `PoiNavigationHolder` espone un nuovo `routingStatus: StateFlow<RoutingStatus?>` (Loading/Success/NoApiKey/Failure), mostrato in `MainMapScreen.kt` come piccolo testo sotto "Stai andando verso: ..." — permette finalmente di vedere, direttamente sul telefono, se e perché il calcolo del percorso reale fallisce (es. `HTTP 401`/`403` chiave non valida, `429` quota esaurita, timeout, ecc.).
    - **Causa 2 — camera che non si sposta mai per includere la destinazione, corretta**: a differenza della navigazione su un percorso GPX caricato (dove la camera si inquadra sull'intero tracciato), per la navigazione verso un luogo senza un percorso reale disponibile la camera non si spostava mai per includere la destinazione — il marker di destinazione poteva restare fuori dall'inquadratura corrente, dando l'impressione che non fosse comparso nulla (anche quando la richiesta funzionava correttamente). Corretto in `TrekMapView.kt`: nuovo parametro `focusOnPoi`, usato da `MainMapScreen.kt` solo quando `mapTrack` è `null` (nessuna linea da disegnare) — inquadra posizione corrente + destinazione una volta sola all'avvio della navigazione (o al cambio di destinazione), non ad ogni fix GPS (che riproporrebbe il problema del "salto" continuo della camera già evitato per la linea retta, vedi punto 13). Quando arriva un percorso reale, `mapTrack` smette di essere `null` e il fit torna a seguire l'intero tracciato come già avveniva.
    - **Nota per il debug futuro**: la mia sandbox di sviluppo non riesce a raggiungere `api.heigit.org` (bloccato dalla policy di rete dell'ambiente), quindi un test diretto della chiamata usata dall'app non è stato possibile da qui — la verifica esterna (playground ufficiale) resta il modo più rapido per isolare "problema della chiave/account" da "problema nell'app" quando serve.
    - **Colore della linea del percorso, quando disegnata** (chiesto esplicitamente): verde scuro `#3D7A64`, spessore 4px (`TrekMapView.kt`, `addTrackLayer`) — lo stesso colore/spessore per qualunque tracciato disegnato sulla mappa (GPX caricato o percorso reale calcolato), nessuna differenza visiva tra i due casi al momento.
    - **Preoccupazione sollevata dall'utente sulla linea d'aria come guida in montagna — valida, da tenere in considerazione**: la linea retta di riserva (usata dal motore di navigazione per freccia/distanza quando manca un percorso reale) punta sempre in linea d'aria verso la destinazione, ignorando ostacoli reali del terreno — esempio concreto fatto dall'utente: potrebbe indicare di guadare un torrente dove un sentiero vero passerebbe invece da un ponte poco distante. Rischio reale, non solo teorico, proprio nel contesto montano a cui è rivolta l'app. Al momento la linea retta non viene disegnata sulla mappa (mitiga in parte, l'utente non la scambia per un percorso reale da seguire visivamente), ma la freccia/distanza mostrate durante la navigazione restano comunque calcolate su di essa, quindi il rischio di percezione resta se l'utente si fida ciecamente della freccia senza guardare il terreno. Opzioni da valutare in futuro, non ancora decise: un avviso esplicito quando la navigazione verso un luogo sta usando solo la linea d'aria (nessuna chiave API configurata o percorso reale non disponibile) del tipo "nessun percorso reale disponibile: la freccia indica la direzione in linea d'aria, presta attenzione a ostacoli come corsi d'acqua e dislivelli"; oppure incoraggiare più esplicitamente la configurazione della chiave API prima di usare questa funzione, proprio per i casi in montagna dove la differenza fra linea d'aria e sentiero reale è più pericolosa.
16. **Segnalato su dispositivo reale (v1.28) — installazione dell'APK di nuovo bloccata, stesso sintomo del blocco già risolto in v1.21**: dopo aver installato le build fino a v1.27 senza problemi, l'installazione della v1.28 è stata rifiutata dal dispositivo — stesso sintomo della v1.20→v1.21 (vedi punto sopra in "Note operative" e la voce corrispondente in `README.md`, "Installazione dell'APK annullata..."), con `versionCode`/`versionName` già verificati corretti dall'utente fin da subito (28/1.28).
    - **Due chiarimenti importanti dell'utente, che correggono le ipotesi scritte qui inizialmente**: (1) build GitHub Actions e download dell'APK sono legati a doppio filo — un file si scarica solo se la Action è verde, e non sarebbe comunque possibile scaricare l'artifact di una run fallita. Questo esclude con certezza un problema di compilazione per la v1.28 (l'ipotesi "verificare se la Action è andata a buon fine" scritta qui ieri sera era superflua: se l'utente ha potuto scaricare il file, la Action era per forza verde). (2) Osservazione empirica dell'utente, coerente con l'intera storia di questo bug: una volta che un tentativo di installazione fallisce per una qualunque ragione, i tentativi successivi con lo **stesso** `versionCode` restano bloccati a prescindere — non importa se quel motivo iniziale aveva a che fare con l'app in sé (un download interrotto, un problema di spazio/momentaneo sul telefono, qualunque causa transitoria) — il gestore pacchetti Android sembra "restare bloccato" su quella combinazione di pacchetto+versione finché il numero non cambia. Se questa lettura è corretta, la causa del blocco iniziale può essere quasi qualunque cosa, non necessariamente un difetto nel codice della v1.28: è il **secondo e i successivi tentativi** falliti con lo stesso version code a essere il vero problema visibile, non il primo fallimento in sé.
    - **Aggiornamento — il solo cambio di versione NON ha risolto stavolta**: l'utente ha provato con un `versionCode` successivo (1.29/1.30, generato da questa stessa sessione) e l'installazione è stata rifiutata di nuovo. **Questo esclude la lettura "basta cambiare versione" come causa universale per questo caso specifico** — a differenza di v1.20→v1.21, dove il solo incremento aveva risolto. Il codice app compilato in questi APK è identico a quello della v1.28 (v1.29/v1.30 hanno toccato solo `docs/PIANO_SVILUPPO.md`, non `app/src`), quindi il sospetto si sposta di nuovo sul codice introdotto proprio in v1.28: `RoutingRepository.kt` (riscritto con `RoutingOutcome`, nuovo import `retrofit2.HttpException`), `PoiNavigationHolder.kt` (nuova sealed class `RoutingStatus` annidata nell'`object`), `MainMapScreen.kt` (nuovo `when` esaustivo su `RoutingStatus`), `TrekMapView.kt` (nuovo parametro `focusOnPoi`, nuova funzione `fitCameraToPoints`).
    - **Aggiornamento — anche un'installazione davvero pulita fallisce identica**: confermato dall'utente — stessi messaggi ("L'app non è stata installata"/"Annullamento"), spazio libero sufficiente, build GitHub Actions verde, solo aggiornamento provato all'inizio; poi test più rigoroso: disinstallazione completa, **riavvio del telefono**, reinstallazione della 1.30 da zero — fallita identica. Questo esclude, con più sicurezza di prima, tutte le cause "storiche" già verificate in questo progetto (firma APK invariata dalla v1.3, `AndroidManifest.xml` non toccato in v1.28, versioni Gradle/AGP/Kotlin fissate, spazio libero, stato residuo di un tentativo precedente). **Osservazione strutturale importante**: la v1.28 non ha aggiunto permessi, dipendenze o modifiche al manifest — solo logica Kotlin nei file elencati sopra e 3 nuove stringhe in `strings.xml`. Il gestore pacchetti Android normalmente rifiuta un'installazione per motivi strutturali (firma, manifest, permessi, versione SDK, spazio, APK corrotto), non per la logica interna del codice Kotlin — rende plausibile un'ipotesi non ancora considerata: **non è affatto un problema di codice**, ma qualcosa di esterno all'app (es. Google Play Protect che blocca silenziosamente un APK sideload non firmato/non verificato, o il file scaricato sul telefono effettivamente corrotto/troncato in un modo che sopravvive anche a un nuovo download se il telefono ne riusa una copia in cache). **Prossimi passi da provare, non ancora fatti**: controllare se Google Play Protect (app Play Store → icona profilo → Play Protect) ha segnalato o bloccato questo file specifico; scaricare di nuovo l'APK da zero (nuova copia, non riusare un file già presente) e verificarne la dimensione; provare a trasferire l'APK via cavo USB da un PC invece di scaricarlo direttamente col browser del telefono, per escludere una corruzione nel download mobile.
    - **Play Protect controllato — "Nessuna app dannosa trovata"**: non ha segnalato né bloccato nulla, ma è la scansione generale periodica del dispositivo (le app già installate), non necessariamente la stessa verifica che scatta durante un singolo tentativo di installazione di un file mai arrivato a installarsi — indebolisce ma non esclude del tutto questa pista.
    - **Dettaglio importante emerso solo ora sul metodo di trasferimento dell'utente**: il flusso non è "scarica direttamente col browser del telefono" (come ipotizzato finora in questo documento), ma **download dell'APK sul PC, poi trasferimento al telefono tramite WeTransfer** — usato per tutte le app installate finora, non solo per questa. Aggiunge due passaggi in più (upload a WeTransfer, poi download sul telefono da WeTransfer) rispetto a un download diretto, ciascuno un potenziale punto di corruzione/troncamento del file non ancora considerato — e un servizio di trasferimento file di terzi potrebbe in teoria comprimere/impacchettare il file diversamente dall'originale. **Test diagnostico proposto, non ancora fatto**: scaricare l'APK di debug direttamente col browser del telefono dalla pagina della run GitHub Actions (bypassando completamente PC e WeTransfer per questo unico tentativo) e provare a installarlo — se funziona, la causa è nel passaggio PC→WeTransfer→telefono, non nel codice dell'app; se fallisce identico anche così, il sospetto torna sul codice introdotto in v1.28.
    - **Test eseguito — fallisce identico anche col download diretto sul telefono**: esclude con certezza l'intera catena di trasferimento (PC, WeTransfer, download mobile) come causa. A questo punto sono state escluse tutte le cause esterne all'app considerate finora (versione, firma, manifest, spazio, trasferimento, scansione periodica Play Protect) — il sospetto torna con più forza sul codice introdotto in v1.28, oppure su un blocco Play Protect *in tempo reale* durante l'installazione (distinto dalla scansione periodica generale già controllata, e non ancora escluso). **Da provare/chiedere alla ripresa**: subito dopo il prossimo tentativo fallito, controllare la tendina delle notifiche per un eventuale avviso di Play Protect relativo a quello specifico file, e verificare se il dialogo di installazione mostra un link "Dettagli"/"Ulteriori informazioni" con un codice di errore più specifico del generico "L'app non è stata installata". **Nessuna notifica trovata**: indebolisce ulteriormente anche l'ipotesi Play Protect in tempo reale, senza escluderla del tutto.
    - **Build diagnostica di controllo (v1.31)**: con ogni causa esterna verificabile da remoto ormai esclusa, unico modo per avere una risposta definitiva è un esperimento controllato — `PoiNavigationHolder.kt`, `RoutingRepository.kt`, `MainMapScreen.kt`, `TrekMapView.kt` e `strings.xml` sono stati riportati **temporaneamente** al contenuto esatto della v1.27 (ultima versione installata con successo), con `versionCode`/`versionName` comunque portati a 1.31 (più alto di qualunque tentativo precedente, per non riproporre il dubbio sul version code). Se questa build si installa, la causa è con certezza nel codice aggiunto in v1.28 (da reintrodurre pezzo per pezzo per isolare la riga esatta). Se fallisce identica anche questa — stesso identico codice dell'ultima versione funzionante, solo con un numero diverso — allora il problema non è mai stato nel codice, ma in qualcosa cambiato sul dispositivo stesso nel frattempo (es. un aggiornamento di sistema Android/Play tra ieri e oggi), e la funzione di instradamento reale (v1.28) andrà ripristinata subito dopo il test, qualunque sia l'esito.
    - **Esito del test — fallisce identico**: stesso identico codice della v1.27 (l'ultima installata con successo in passato), stesso identico blocco. **Conclusione definitiva: la causa non è mai stata nel codice dell'app**, in nessuna versione. Il codice v1.28 (instradamento reale) è stato subito ripristinato (v1.32), dato che non ha alcuna responsabilità in questo problema.
    - **Nuovo dettaglio del sintomo, più preciso di quanto descritto finora**: non è il classico dialogo "L'app non è stata installata"/"Annullamento" (quello era il sintomo del caso v1.20→v1.21, risolto col cambio versione). Il sintomo attuale è diverso: appare la banda grigia, **Play Protect scansiona l'app e completa la verifica senza trovare problemi**, il pulsante "Installa" si attiva normalmente — ma premendolo non succede nulla: nessuna barra di progresso, nessun messaggio, nessun errore, la schermata semplicemente sparisce. Pattern noto e documentato online per il "Programma di installazione pacchetti" di Android: tipicamente causato da cache/dati corrotti del Play Store o del Programma di installazione pacchetti stesso, non da un difetto nel file APK.
    - **Prossimi passi consigliati, lato dispositivo (non richiedono modifiche al codice)**: (1) Impostazioni → App → mostra app di sistema → "Programma di installazione pacchetti" (o nome simile) → Archiviazione → Cancella cache (e se non risolve, anche Cancella dati); (2) stessa cosa per l'app "Google Play Store" → Archiviazione → Cancella cache; (3) se possibile, disattivare temporaneamente "Scansiona le app con Play Protect" (Play Store → icona profilo → Play Protect → ingranaggio impostazioni), riprovare l'installazione, poi riattivarla; (4) verificare se c'è un aggiornamento di sistema Android in sospeso (Impostazioni → Sistema → Aggiornamento software) che potrebbe lasciare il gestore pacchetti in uno stato incoerente finché non viene installato; (5) provare un riavvio in modalità provvisoria (Safe Mode) e tentare l'installazione da lì, per escludere interferenze di altre app (antivirus, gestori file) sul processo di installazione.
    - **Cancellata la cache di "Programma di installazione pacchetti" — il sintomo è cambiato**: prima (banda grigia, Play Protect verificava senza problemi, "Installa" attivo ma senza effetto premendolo) ora, dopo la pulizia cache, compare invece la banda grigia con "Annullamento" e la verifica non parte nemmeno più — sintomo diverso, più simile a quello storico del caso v1.20→v1.21 (tipicamente un secondo tentativo sullo stesso file già provato). Segno positivo: la pulizia della cache ha effettivamente cambiato lo stato dell'installer, quindi probabilmente è la strada giusta. Prossimo tentativo proposto: cancellare anche i "Dati" (non solo la cache) di Programma di installazione pacchetti, poi scaricare una copia nuova dell'APK con versione aggiornata (v1.33) e riprovare — combinando "installer ripulito" con "file mai visto prima da quell'installer", per non lasciare margine di dubbio su quale dei due fattori serva davvero.
    - **"Dati utente" dell'installer già a 0**: non c'era nulla da cancellare oltre alla cache — la pulizia della sola cache resta l'unica azione che ha avuto un effetto osservabile.
    - **Test con la v1.33 (file nuovo, cache pulita) — fallito di nuovo, con il sintomo tornato quello precedente**: non "Annullamento" (visto una volta sola, subito dopo la pulizia cache, probabilmente legato al fatto che quel tentativo riusava ancora il file della versione precedente), ma di nuovo banda grigia → Play Protect verifica senza trovare nulla → "Installa" attivo → nessun effetto premendolo, schermata che sparisce. Il sintomo quindi oscilla fra due varianti a seconda dello stato recente dell'installer, ma l'esito (fallimento) è costante, indipendente da: contenuto del codice (v1.31), numero di versione (più volte), metodo di trasferimento, scansione Play Protect, pulizia cache installer. Tutte le vie "a basso sforzo" sono ormai esaurite senza risultato.
    - **Prossimo test decisivo proposto, non ancora fatto**: provare a installare un APK completamente estraneo a questo progetto (qualunque altro file .apk legittimo disponibile, anche scaricato al momento da una fonte affidabile) per capire se il sideload è rotto in generale su questo dispositivo in questo momento, non solo per GM-Trekking — se fallisce anche quello, il problema è sicuramente del dispositivo stesso (non risolvibile da qui, servirebbe assistenza Android/produttore) e non ha più senso continuare a modificare versione o codice di questo progetto per inseguirlo. In alternativa/parallelo, se l'utente ha ancora salvato da qualche parte un vecchio file APK di GM-Trekking installato con successo in passato (prima di oggi), riprovare a installare esattamente quel file — se anche quello ora fallisce, conferma ulteriormente che il problema è del dispositivo, non del progetto.
    - **Test proposti dall'utente stesso, in corso**: (1) installare l'aggiornamento di un'altra app sviluppata con la stessa tecnologia (Kotlin/Compose) — stessa idea del test "APK estraneo" sopra, ma con un'app dell'utente stesso, probabilmente più comoda da provare; (2) installare GM-Trekking su una VM Android — isola completamente l'APK da qualunque particolarità del dispositivo fisico: se si installa sulla VM, il file è certamente valido e il problema è specifico del telefono fisico in questo momento.
    - **Ipotesi overlay/tapjacking (ricerca web) — verificata e ESCLUSA**: cercati online sintomi simili ("pulsante Installa che non fa nulla, nessun errore"); il candidato più plausibile trovato era il blocco di sicurezza anti-tapjacking di Android, che ignora silenziosamente il tocco sul pulsante "Installa" quando un'altra app disegna un overlay sopra lo schermo (filtri luce blu, dimmer, pulsanti di gesture flottanti, ecc. — verificabile in Impostazioni → App → Accesso speciale → "Visualizza sopra altre app"). Controllato dall'utente: solo YouTube aveva quel permesso attivo, disattivato senza alcun effetto sul blocco — ipotesi esclusa.
    - **Comportamento non costante, dettaglio importante**: il messaggio finale varia da un tentativo all'altro senza un pattern chiaro legato alle azioni fatte — a volte solo "Annullamento", altre volte "L'app non è stata installata" — e in entrambi i casi, più recentemente, la fase di verifica/scansione del file (vista nei tentativi precedenti) non compare più affatto. Questa incostanza è di per sé un'informazione: esclude quasi certamente una causa fissa e deterministica (una singola impostazione, un overlay specifico, un blocco puntuale) e punta piuttosto verso qualcosa di sistemico/instabile sul dispositivo in quel momento — es. pressione sulla memoria RAM disponibile, Google Play Services in uno stato incoerente, o servizi di sistema sotto carico variabile. Non ha più senso rincorrere una singola causa precisa con altri tentativi mirati: i due test già in corso proposti dall'utente (aggiornamento di un'altra sua app, installazione su VM Android) restano la via più informativa.
    - **Risultato del primo test dell'utente — conclusivo**: l'aggiornamento di un'altra app sua (sviluppata qui, stessa tecnologia Kotlin/Compose, codice completamente indipendente da GM-Trekking) è fallito con lo stesso identico sintomo. Nello stesso momento, altre app si sono aggiornate senza problemi **dal Play Store**. **Conclusione: il problema riguarda specificamente il percorso di installazione "sideload" (file APK installato manualmente, fuori dal Play Store) su questo dispositivo, non un'app in particolare né il dispositivo in generale.** Dispositivo: Motorola Edge 20, Android 13 — lo stesso già coinvolto nel caso v1.20→v1.21 (vedi sopra).
    - **Osservazione dell'utente sul momento di comparsa**: il problema, in questa gravità, si è manifestato solo dopo un aggiornamento di GM-Trekking — suggerisce che un tentativo di installazione di GM-Trekking in un momento imprecisato abbia lasciato il gestore pacchetti del telefono (non l'app in sé) in uno stato incoerente che poi si ripercuote su qualunque sideload successivo, di qualunque app. Coerente con un problema noto su Android: sessioni di installazione interrotte/bloccate che restano "incagliate" nel `PackageInstaller`/`PackageManagerService` e impediscono nuove installazioni finché non vengono ripulite, indipendentemente dall'APK specifico.
    - **Prossimi passi proposti**: (1) più semplice — cancellare cache **e dati** di tre app insieme (non solo Programma di installazione pacchetti): anche "Google Play Store" e "Servizi Google Play", poi riavviare il telefono e riprovare; (2) più efficace per avere una diagnosi certa — collegare il telefono al PC via USB, attivare "Debug USB" (Impostazioni → Info telefono → tocca 7 volte "Numero build" per sbloccare Opzioni sviluppatore → Debug USB), installare sul PC solo `platform-tools` di Android (un pacchetto piccolo, separato da Android Studio, solo il comando `adb`) e lanciare `adb install nome-file.apk` da terminale: a differenza del dialogo a schermo, la console di `adb` mostra il vero codice di errore Android (es. `INSTALL_FAILED_INSUFFICIENT_STORAGE`, `INSTALL_FAILED_INVALID_APK`, `INSTALL_FAILED_ABORTED`...), che chiarirebbe la causa reale invece di continuare a ipotizzare.
    - **RISOLTO come workaround affidabile — installazione via `adb install` da USB riuscita**: l'utente ha collegato il telefono al PC, attivato il Debug USB e installato l'APK con `adb install` (platform-tools di Google) — installazione riuscita al primo colpo. Conferma definitiva che il file APK è valido in ogni sua parte (codice, firma, manifest, build) e che il blocco riguarda specificamente il percorso grafico di installazione (l'app "Programma di installazione pacchetti" quando si apre il file scaricato) — `adb install` passa per lo stesso meccanismo di sistema ma non attraversa quella specifica interfaccia/Activity, e lì evidentemente restava incagliato qualcosa. **Causa esatta del blocco nel percorso grafico**: resta un mistero aperto, non ulteriormente indagato — non più urgente, dato che esiste ora un modo affidabile per installare gli aggiornamenti (via USB/adb) ogni volta che serve, incluso in vista dell'uso reale dell'app durante un cammino in montagna (agosto 2026).

#### Spunti dalla ricerca competitiva (Komoot, AllTrails, Wikiloc, Gaia GPS — agosto 2026)

- **Tempo totale vs tempo in movimento**: Wikiloc mostra entrambi separatamente. Più utile del solo tempo totale (non gonfiato dalle soste) — da adottare per il punto 1.
- **Dislivello — barometro + GPS ibrido**: il solo GPS sovrastima parecchio il dislivello (rumore verticale, es. +90m su un tratto pianeggiante); un sensore barometrico (se presente sul telefono, `SensorManager.TYPE_PRESSURE`) è molto più preciso ma va calibrato sull'altitudine GPS di partenza e deriva con il meteo. Approccio consigliato: usare il barometro quando disponibile, calibrato a inizio percorso, con fallback al solo GPS (filtrato/smussato) se assente.
- **Registrazione libera, non solo su GPX**: Gaia GPS, HiiKER, Footpath e altre permettono di premere "Avvia" e registrare senza una traccia precaricata — esattamente il caso "solo partenza/arrivo" del punto 1, non un'eccezione da gestire a parte.
- **Foto come caso particolare di waypoint**: Gaia GPS non tratta le foto come una funzione separata dalle note puntuali — una foto è un waypoint con un'icona e, opzionalmente, un'immagine allegata. Conviene modellare punto 2 e la nota puntuale del punto 3 sulla stessa struttura dati (stesso "punto sul percorso", con testo e/o foto opzionali), invece di due sistemi paralleli.
- **Icone per categoria sui punti**: Gaia GPS usa icone predefinite per tipo di punto (pericolo, acqua, bivio, campeggio...) invece di un unico marker generico — rende la mappa leggibile a colpo d'occhio ed è coerente con il principio guida "sicurezza e chiarezza" già alla base del progetto.
- **Nota generale del percorso — occasione di differenziazione**: AllTrails delega esplicitamente il diario di viaggio (meteo, compagni, sensazioni) a un'app esterna, non lo integra. Farlo bene direttamente in GM-Trekking (punto 3) non è solo "mettersi alla pari" con la concorrenza ma un vero punto di distinzione.

## Fase 3 — Community e arricchimento dati (3-4 mesi dopo la Fase 2)

- Condivisione di percorsi tra utenti.
- Segnalazioni della community su condizioni del sentiero o correzioni ai luoghi utili.
- Sincronizzazione multi-dispositivo.
- Eventuale integrazione di fonti dati POI aggiuntive (es. Google Places) per arricchire orari, recensioni, foto.
- Valutazione di un modello di monetizzazione (freemium), solo dopo aver validato l'adozione.

## Architettura tecnica

- **Piattaforma**: Android nativo, Kotlin + Jetpack Compose (scelto invece di un framework cross-platform perché il target è solo Android e la gestione fine di GPS in background/consumo batteria richiede accesso diretto alle API di sistema).
- **Mappe**: MapLibre Android SDK, tile OpenStreetMap — nessun costo di licenza legato al volume di utilizzo.
- **Dati luoghi utili**: OpenStreetMap tramite Overpass API (gratuita, nessuna chiave richiesta).
- **Localizzazione in background**: Foreground Service Android con tipo `location` dichiarato (obbligatorio da Android 14 in poi).
- **Persistenza locale**: da introdurre in Fase 2 (Room) per la cache offline dei luoghi utili; nello scheletro attuale i dati POI sono tenuti in memoria per semplicità.
- **Compilazione**: GitHub Actions genera l'APK ad ogni push sul branch `main` (vedi `.github/workflows/android-build.yml`); l'APK compilato è scaricabile dagli Artifacts della run, senza dover installare Android Studio per ottenere un file installabile.

## Note operative (indicazioni dirette dell'utente, da rispettare in ogni sessione)

- **Tema grafico**: tema bianco per le schermate dell'app. ✅ Fatto — vedi `ui/theme/Theme.kt` e `Color.kt`.
- **Coerenza ad ogni modifica**: ad ogni modifica ai file del progetto, verificare che tutto resti coerente e che non manchi nulla (riferimenti tra file, dipendenze usate ma non dichiarate o viceversa, versioni allineate) prima di considerare il passo concluso.
- **Numero di versione — incrementare ad ogni modifica che richiede un nuovo APK, non ad ogni commit** (regola precisata agosto 2026, dopo due iterazioni — vedi caso reale e correzione sotto): la versione attuale del progetto è la **1.34** (`versionName` in `app/build.gradle.kts`). Il criterio è: la modifica richiede la generazione di un nuovo APK da installare per essere verificata (qualunque file sotto `app/src`, `app/build.gradle.kts` a parte il numero stesso, risorse, manifest)? Se sì, incrementare `versionCode` (+1) e `versionName`. Se la modifica riguarda **solo** file di appunti/documentazione interna (`docs/PIANO_SVILUPPO.md`, `README.md`) senza alcun effetto sul programma, **non** incrementare — non c'è un nuovo APK da testare, quindi nessun rischio del blocco descritto sotto. Il numero di versione deve anche essere mostrato nell'interfaccia dell'app (non ancora fatto): `buildFeatures.buildConfig = true` è già abilitato nel Gradle, quindi basterà leggere `BuildConfig.VERSION_NAME` — punto d'ingresso naturale: un piccolo testo nella barra in alto di `MainMapScreen.kt`, o una futura schermata "Informazioni".
  - **Caso reale che ha portato alla prima versione di questa regola (v1.19-v1.21, agosto 2026)**: dopo aver aggiornato "Impostazioni" (v1.20), l'installazione dell'APK su un dispositivo reale veniva annullata immediatamente all'apertura del file ("L'app non è stata installata", poi "Annullamento" ai tentativi successivi) — anche disinstallando completamente l'app prima di reinstallarla da zero, e anche con un build "diagnostico" senza alcuna modifica funzionale (stesso `versionCode` del build precedente). Verificate e scartate tutte le altre cause possibili: firma dell'APK, `AndroidManifest.xml`, dipendenze e versioni dei plugin Gradle (tutte fissate, invariate), integrità del repository Git. **Risolto incrementando `versionCode`/`versionName` (a 1.21) senza nessun'altra modifica** — su indicazione esplicita dell'utente, che aveva già affrontato lo stesso identico sintomo in un altro progetto: a quanto pare, su alcuni dispositivi/versioni Android il gestore dei pacchetti può bloccare silenziosamente l'installazione di un nuovo APK se il `versionCode` non cambia rispetto all'ultimo tentativo, anche quando si tratta di un'installazione pulita senza alcuna versione precedente installata (un comportamento non documentato ufficialmente, osservato empiricamente).
  - **Prima correzione (v1.22, agosto 2026), poi ulteriormente precisata**: dopo il caso sopra, la regola era stata estesa a "incrementare ad ogni commit, senza eccezioni, anche solo di documentazione" — troppo ampia. **Seconda correzione (v1.31, agosto 2026), su indicazione esplicita dell'utente**: il version code va cambiato solo quando la modifica genera davvero un nuovo APK da installare, non ad ogni commit in senso assoluto — appunti interni su idee/lavori da fare non lo richiedono. Osservazione aggiuntiva dell'utente sul meccanismo del blocco: una volta che un tentativo di installazione fallisce per una qualunque ragione (anche transitoria, non necessariamente un difetto del codice), i tentativi successivi con lo **stesso** `versionCode` restano bloccati a prescindere — è quindi il secondo tentativo (e successivi) con versione invariata a fallire in modo visibile, non necessariamente il primo motivo scatenante. Da qui il criterio pratico: **incrementare prima di ogni build che l'utente scaricherà e installerà per testare qualcosa**, non come conteggio dei commit in sé.
- **Messaggi di commit, sempre con la versione davanti**: ogni commit deve iniziare con `vX.Y - descrizione` (es. `v1.11 - ...`), usando l'ultimo numero di versione valido — se il commit è solo di documentazione e non lo incrementa (vedi punto sopra), il prefisso resta quello dell'ultima versione app effettivamente cambiata, non un numero nuovo inventato per l'occasione.
- **Workflow di sviluppo**: l'utente non usa Android Studio, solo GitHub (push → GitHub Actions compila l'APK → installazione diretta sul telefono). Le richieste di log/diagnostica vanno quindi pensate senza presumere accesso a Logcat/adb — da qui la schermata di crash in-app (`crash/CrashHandler.kt`).
- **Ogni schermata deve avere un modo esplicito di tornare alla mappa principale**: freccia/pulsante indietro visibile in alto, non affidarsi solo al gesto/pulsante di sistema — coerente con il principio di chiarezza per utenti non esperti già alla base del progetto. [x] **fatto (v1.12)**: `PlacesScreen.kt` (retrofit) e le nuove `ActivityHistoryScreen.kt`/`ActivityDetailScreen.kt` hanno tutte una freccia indietro esplicita (`navigationIcon` nella `TopAppBar`, popBackStack verso la schermata precedente). Standard da applicare anche a tutte le schermate future (Emergenza, Impostazioni, ecc.) fin dalla loro prima versione.

## Rischi da monitorare (ripresi dall'analisi di fattibilità)

- Qualità/copertura disomogenea dei dati OpenStreetMap in alcune aree montane: mitigare permettendo segnalazioni dagli utenti in Fase 3.
- Consumo batteria durante tracciamento lungo: testare su più dispositivi reali, non solo emulatore.
- Precisione GPS in bosco/valli strette: la soglia di tolleranza per l'avviso di fuori percorso deve essere calibrabile e testata sul campo, non solo a tavolino.
- Versioni delle dipendenze (Kotlin, Compose, MapLibre) verificate ad agosto 2026: se Android Studio propone aggiornamenti (Upgrade Assistant), è normale e sicuro accettarli.
- **Dipendenza da infrastruttura Overpass API pubblica — mitigata con un fallback multi-mirror (v1.17)**: sia "Luoghi utili" sia "Sentieri vicini" si affidavano a un unico mirror fisso (`overpass.kumi.systems`), senza alcun tentativo di riserva — segnalato dall'utente come un fallimento/timeout frequente per entrambe le funzionalità (agosto 2026). Corretto in `data/poi/OverpassApiService.kt` e nuovo `data/poi/OverpassQueryExecutor.kt`: ora si prova in sequenza fino a 3 mirror pubblici (un solo tentativo per mirror, non ripetuto sullo stesso — un server sovraccarico fallisce quasi sempre di nuovo allo stesso modo pochi secondi dopo, quindi passare subito al successivo è meglio che allungare l'attesa peggiore), condiviso da `PoiRepository.kt` e `TrailRepository.kt` tramite un parametro iniettabile (`queryExecutor`, di default `OverpassQueryExecutor::execute`).
  - **Ordine dei mirror** (dal preferito all'ultima risorsa): `overpass.private.coffee` (nome attuale dello stesso gestore/hardware già usato come `overpass.kumi.systems` — il dominio è cambiato nel 2026, 4 server da 20 core/256 GB RAM ciascuno, nessun limite di richieste dichiarato); `overpass-api.de` (istanza ufficiale principale, 2 server da 16 core/128 GB RAM, quota generosa — usata come riserva e non come principale per prudenza, dato il precedente HTTP 406 di questo stesso server verso l'app, mitigato dagli header già inviati su ogni richiesta); `maps.mail.ru/osm/tools/overpass` (mirror russo VK Maps, hardware ancora più forte, nessun limite dichiarato — ultima risorsa per la latenza potenzialmente più alta dall'Europa). Ordine stabilito dai dati ufficiali della pagina "Overpass API" del wiki OpenStreetMap (agosto 2026) più l'esperienza reale già fatta in questo progetto — non da una misura diretta di velocità dei mirror, perché l'ambiente usato per scrivere questo codice non ha accesso di rete verso domini esterni arbitrari (stesso limite già incontrato per altre verifiche in questo progetto, vedi punto 7).
  - **Pulsante "Riprova"** aggiunto nello stato di errore di `PlacesScreen.kt` e `NearbyTrailsScreen.kt`: prima bisognava uscire e rientrare nella schermata per un nuovo tentativo.
  - **Query di "Sentieri vicini" alleggerita**: raggio di ricerca ridotto da 5 a 3 km (`TrailRepository.findNearby`/`NearbyTrailsViewModel.loadNearby`) — la combinazione di un raggio ampio e di `out geom` (geometria completa di ogni sentiero) rendeva questa query più pesante di quella di "Luoghi utili" e più vicina al limite dei 25s dichiarati lato server nelle zone con molti sentieri CAI/GR sovrapposti, un fattore di timeout concreto indipendente dall'affidabilità del mirror in sé.
  - Soluzione strutturale a lungo termine, se il problema continuasse a ripetersi anche con queste mitigazioni: scaricare periodicamente un estratto `.osm.pbf` (es. da Geofabrik) e servire i dati da una copia propria invece che da server Overpass pubblici condivisi.
