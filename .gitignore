*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
*.hprof
.kotlin/

# Keystore (non versionare mai le chiavi di firma)
*.jks
*.keystore

# Gradle wrapper jar: generato localmente da Android Studio o da `gradle wrapper`
# (vedi README.md — non incluso in questo scheletro perché binario)
